apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: remote-xcomm-test-v1
  namespace: remote-xcomm
spec:
  template:
    spec:
      containers:
      - image: whonamihwp/remote-xcomm-test
        ports:
        - name: h2c
          containerPort: 8081

